<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= typeof title !== 'undefined' ? title + ' – The Flyer' : 'The Flyer' %></title>
<link rel="stylesheet" href="/static/styles/style.css">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=DM+Mono:ital,wght@0,300;0,400;0,500;1,300;1,400;1,500&family=Space+Grotesk:wght@300..700&display=swap" rel="stylesheet">
<script>
    // Apply night mode immediately from localStorage to prevent flash
    if (localStorage.getItem('nightMode') === 'true') {
        document.documentElement.classList.add('night-mode');
        // Swap logo once DOM is ready
        document.addEventListener('DOMContentLoaded', () => {
            const logo = document.getElementById('site-logo');
            if (logo) logo.src = '/static/img/flyerLogo_night.svg';
        });
    }
</script>
